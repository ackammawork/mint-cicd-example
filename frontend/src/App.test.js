// frontend/src/App.test.js
import { render, screen } from '@testing-library/react';
import App from './App';

// test('renders Create Item text', () => {
//   render(<App />);
//   const linkElement = screen.getByText(/Create Item/i);
//   expect(linkElement).toBeInTheDocument();
// });
